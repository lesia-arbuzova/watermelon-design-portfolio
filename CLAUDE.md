@RTK.md

# Claude Code project rules

This static portfolio repo must use the shared RTK policy in `RTK.md` for all Bash commands. Prefer Claude Read for full-file inspection. Do not use full-file `cat` or unbounded `rtk read <file>`; use bounded reads or file tools.
